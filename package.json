{
  "name": "fassjs-utils",
  "version": "0.0.0-alpha.0",
  "license": "MIT",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "lint": "tslint -p tsconfig.json --fix",
    "build": "tsc",
    "test": "jest --config jestconfig.json --expand",
    "prepare": "yarn test && yarn lint && yarn build && yarn pack && tar zxvf *.tgz"
  },
  "dependencies": {
    "winston": "*"
  },
  "devDependencies": {
    "@babel/core": "*",
    "@babel/preset-env": "*",
    "@babel/preset-typescript": "*",
    "@types/node": "*",
    "@types/jest": "*",
    "@types/debug": "*",
    "typescript": "*",
    "tslint": "*",
    "jest": "*",
    "babel-jest": "*"
  }
}